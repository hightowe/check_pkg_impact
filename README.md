# check_pkg_impact
Interrogate the impact of upgrading deb packages on running programs

Programs to try to determine, in advance, if a deb package that is a
candidate for upgrade will impact any running processes and, if so, to
list those. The goal is to make that research more convenient and accurate
than doing it by hand.
